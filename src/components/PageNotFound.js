import React from 'react';
/* 
    Renders when there is a 404 request in the app
*/
const PageNotFound = () => (
  <div>
    <h1>Whoops! 404 - The requested page does not exist.</h1>
    <p>Perhaps try one of the suggested search buttons?</p>
  </div>
);

export default PageNotFound;